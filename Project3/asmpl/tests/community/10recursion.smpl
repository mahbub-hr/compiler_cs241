main
var x, y;
array [5] a;

function factorial(num);
var result;
{
  if num == 0 then
      let result <- 1;
    else
      let result <- num * call factorial(num - 1);
    fi;
    return result;
  };

{
  let x <- 5;
  let y <- 0;


  let y <- call factorial(x);
  call OutputNum(y);
}.