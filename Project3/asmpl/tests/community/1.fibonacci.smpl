main
var a, b, c, tmp;
{
    let a <- 10;
    let b <- 0;
    let c <- 1;

    while a > 0 do
        call OutputNum(b);
        call OutputNewLine;
        let tmp <- c;
        let c <- b + c;
        let b <- tmp;
        let a <- a - 1;
    od;
}.