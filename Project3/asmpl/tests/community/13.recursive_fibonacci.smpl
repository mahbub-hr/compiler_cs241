main
var out;
function Fibonacci(n);
{
    if n <= 2 then
        return n;
    fi;

    return call Fibonacci(n - 1) + call Fibonacci(n - 2);
};
{
    let out <- call Fibonacci(call InputNum());
    call OutputNum(out);
}.