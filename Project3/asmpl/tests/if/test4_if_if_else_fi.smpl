main
var a, b, c; {
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    
    if b < 0 then 
        let b <- b + 1;
        
        if c < 0 then
            let c <- c+ 1
        
        else
            let a <- a +1
        fi;

        let d <- b + 1;
    fi;
   
    
    call OutputNum(a);
    call OutputNum(b);
    call OutputNum(c);
}