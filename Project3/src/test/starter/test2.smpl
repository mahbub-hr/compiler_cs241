main
var a, b, c, d, e; {
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    if b < 0 then 
        let b <- b + 1;
        if c < 0 then
            let c <- c+ 1
        fi;

        let a <- a + 1;
        let d <- b + 1;
    fi;
    
    call OutputNum(a);
    call OutputNum(b);
    call OutputNum(c);
}