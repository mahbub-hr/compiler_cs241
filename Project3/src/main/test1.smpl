main
var a, b, c, d, e; {
    let a <- call InputNum();
    let b <- a;
    let c <- b;
    let d <- b + c;
    let e <- a + b;

    if a<0  then
        if b < 0 then let a <- 0 else let a <-0 fi;
    let d <- d + e ; let a <- d else let d <- e 
    
    fi;

    call OutputNum(a)
}