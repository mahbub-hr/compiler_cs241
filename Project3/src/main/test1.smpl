main
var a, b, c, d, e; {
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    while a < 100 do
        if b < 0 then
            let b <- b + 1
            if c < 0 then
                c <- c + 1
            fi;
        fi;
        a <- a + 1; 
        b <- b + 1;
    od;
    call OutputNum(a)
}