main
var a, b, c, d, e; {
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    while a < 100 do
        while b < 0 do 
            let b <- b + 1;
        od;

        let a <- a + 1;
    od;
    
    call OutputNum(a);
    call OutputNum(b);
    call OutputNum(c);
}