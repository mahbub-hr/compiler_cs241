main
var a, b, c, d, e; {
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    while a < 100 do
        let a <- a + 1;
        if d < 0 then 
            let b <- b + 1;
            if c < 0 then
                let c <- c+ 1
            fi;

            let d <- b + 1;
        fi;
    od;
    
    call OutputNum(a);
    call OutputNum(b);
    call OutputNum(c);
}